import { StdDirective } from './std.directive';

describe('StdDirective', () => {
  it('should create an instance', () => {
    const directive = new StdDirective();
    expect(directive).toBeTruthy();
  });
});
